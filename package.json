{
  "name": "headless-wc",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "dev": "grunt dev:ftp",
    "dev:ftp": "grunt dev:ftp",
    "dev:local": "grunt dev:local --output='/Users/dawid/Local Sites/checkout-field-validator/app/public/wp-content/plugins/'",
    "build": "grunt build",
    "deploy": "svn add * --force && svn commit -m 'update' && yarn build && yarn git",
    "git": "git add . && git commit -m 'update' && git push origin main",
    "svnignore": "svn propedit svn:ignore ."
  },
  "devDependencies": {
    "grunt": "^1.6.1",
    "grunt-contrib-compress": "^2.0.0",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-watch": "^1.1.0",
    "grunt-ftp-push": "^1.2.1",
    "grunt-replace": "^2.0.2",
    "grunt-sftp-deploy": "^0.2.5"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
